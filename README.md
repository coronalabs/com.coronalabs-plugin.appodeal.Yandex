Sources for the plugin `plugin.appodeal.Yandex`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.appodeal.Yandex" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
